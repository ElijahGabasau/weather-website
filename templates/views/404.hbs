<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link href="/css/styles.css" rel="stylesheet">
  </head>

  <body>
    <div class="main-content">
      {{>header}}

      <p>{{code}}</p>
      <p>{{title}}</p>
    </div>

    {{>footer}}
  </body>
</html>